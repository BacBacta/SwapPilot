{
  "name": "@swappilot/contracts",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "deploy:pilot:bsc": "hardhat run scripts/deploy-pilot.ts --network bsc",
    "deploy:pilot:testnet": "hardhat run scripts/deploy-pilot.ts --network bscTestnet",
    "verify": "hardhat verify"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^4.0.0",
    "@openzeppelin/contracts": "^5.0.0",
    "hardhat": "^2.19.0",
    "dotenv": "^16.3.1"
  }
}
