{
  "name": "@swappilot/adapters",
  "private": true,
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsup src/index.ts src/types.ts --format cjs --dts --tsconfig tsconfig.build.json --out-dir dist --clean",
    "lint": "eslint .",
    "typecheck": "tsc -p tsconfig.json --emitDeclarationOnly --declaration --declarationMap",
    "test": "vitest run --passWithNoTests"
  },
  "dependencies": {
    "@swappilot/shared": "workspace:*",
    "viem": "^2.23.6",
    "zod": "^4.1.5"
  },
  "devDependencies": {
    "@types/node": "^24.3.0",
    "tsup": "^8.5.0",
    "typescript": "^5.8.3",
    "vitest": "^3.2.4"
  }
}
